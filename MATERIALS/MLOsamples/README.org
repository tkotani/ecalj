still preliminary version. Only for LDA (not QSGW because MTO basis changed --->sigm inconsistency).

1. run lmf-MPIK 
   Get convergence.
2. run job_band

3. modify ctrl file EH and RSMH 
   Si ===> RSMH=1.6 (better with EH=-0.5 probably)
   NiO ===> 
    ATOM=Ni       
      EH=-.5 -.5 -1 -1  RSMH=1.56 1.56 1.06 1.06 
      EH2=-2 -2 -2  RSMH2=1.06 1.06 1.06 
    ATOM=O Z=8 R=1.70    
      MMOM=0 0 0 0
      EH=-1 -1 -1  RSMH=0.85 0.85 0.85
      EH2=-2 -2  RSMH2=0.85 0.85 

4. run job_ham1
   In advance, edit lmfham1.f90 (called from job_ham1),
   to set basis for MLOs.
===this is the part to be edited in lmfham1.f90 ==================
    nnn=0
    do i=1,ldim !MLO dimension 
       if(l_table(i)>=3) cycle !only spd
       if(k_table(i)==2) cycle !only EH part, throw away EH2
!       if( (ib_table(i)==3.or.ib_table(i)==4).and.l_table(i)>=2) cycle !for Oxygen of NiO. skip 3d
!
!       if( (ib_table(i)==3.or.ib_table(i)==4).and.l_table(i)==0) cycle
!       if(l_table(i)>=2) cycle 
!       if(k_table(i)>=2.and.l_table(i)==1) cycle
       write(6,*) 'ham1 index', i,ib_table(i),l_table(i),k_table(i)
       nnn=nnn+1
       ix1(nnn)=i
    enddo
=================================================================

5. plot and check.
   step 4 created band_lmfham_spin1.dat, so we plot MLO band superposed on original bands.
==========================
plot \
"bnd001.spin1" u ($2):($3) lt 1 pt 1 w l,\
"bnd002.spin1" u ($2):($3) lt 1 pt 1 w l,\
"bnd003.spin1" u ($2):($3) lt 1 pt 1 w l,\
"bnd004.spin1" u ($2):($3) lt 1 pt 1 w l,\
"bnd005.spin1" u ($2):($3) lt 1 pt 1 w l,\
"band_lmfham_spin1.dat" u ($1):(13.605*($2-ef)) pt 2 lc 'red' w p 
