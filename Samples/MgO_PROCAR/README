Example of Weight decomposition plot.

We have --mkprocar option (with --band:fn=syml).
For Band weight plot.
---------------------------
See ./jobprocar
And then run ./jobprocar
  After PROCAR is generated by lmf --mkprocar --band mode (only lmf, not yet in lmf-MPIK).
  Run BandWeight.py
  And the do gnpolot bnds.gnu.mgoW
-------------------------
This is only the example for O(p) component.
